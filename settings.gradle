pluginManagement {
    repositories {
        mavenCentral()
        google()
        maven { url 'https://jitpack.io' }
    }
}
// Exclude AndrOBD to avoid conflicts with HurtecOBD2
// include ':androbd', ':library', ':plugin'
include ':library', ':plugin'

